<div class="admin">
<h1><I>User list screen of management screen</h1><I>
<% @users.each do |user| %>
Name : <%= user.name %><br>
Email :<%= user.email %><br>
Tasks :<%= user.tasks.count %><br>
<div class="mgtpage">
       <td><%= link_to 'Edit', edit_user_path(user) %></td>
       <td>
        <%= link_to 'show', user_path(user) %>
    </td>
       <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
     </tr>
     </div>
   <% end %>
 </tbody>
</table>
<br>
<%= link_to 'New User', new_user_path %>
</div>
</div>